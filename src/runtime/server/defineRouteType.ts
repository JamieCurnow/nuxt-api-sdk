/**
 * Use to export the type of an input from the route handler
 * for use in typing the API sdk.
 *
 * @example
 * export const body = defineRouteType<{ input: string }>()
 */
export const defineRouteType = <T>() => null as unknown as T
